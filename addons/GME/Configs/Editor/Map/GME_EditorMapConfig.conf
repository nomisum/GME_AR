// GME editor map config — appends GME_GMEditorMapOverlayUIComponent to the
// vanilla editor map's UI component list.
//
// STUB — this file is incomplete by design. To finish:
//   1. In Workbench, open the vanilla editor map config under base addon
//      58D0FB3206B6F859. Likely path: Configs/Editor/Map/EditorMapConfig.conf
//      (confirm exact path; export it as the reference baseline).
//   2. Copy its `MapConfig` block here verbatim, including the existing
//      `MapUIComponents` array.
//   3. Append a single entry to that array:
//          GME_GMEditorMapOverlayUIComponent "<NEW_GUID>" {
//          }
//   4. Mod the editor camera prefab so its `MapConfiguration` field references
//      the GUID of THIS config instead of vanilla's. (Alternative: mod
//      SCR_MapEntity to substitute on load.)
//
// Without those steps, the GM editor map will not register our overlay
// component at runtime.

// TODO: paste vanilla MapConfig block here, then add overlay entry below.
//
// MapConfig "<GUID>" {
//     ...
//     MapUIComponents {
//         ...vanilla components...
//         GME_GMEditorMapOverlayUIComponent "<NEW_GUID>" {}
//     }
// }
