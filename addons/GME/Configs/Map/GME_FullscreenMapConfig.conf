// GME fullscreen player map config — appends GME_GMFullscreenMapOverlayUIComponent
// to the vanilla fullscreen map's UI component list.
//
// STUB — this file is incomplete by design. To finish:
//   1. In Workbench, open the vanilla fullscreen map config under base addon
//      58D0FB3206B6F859. Likely path: Configs/Map/MapConfig.conf
//      (confirm exact path; export it as the reference baseline).
//   2. Copy its `MapConfig` block here verbatim, including the existing
//      `MapUIComponents` array.
//   3. Append a single entry to that array:
//          GME_GMFullscreenMapOverlayUIComponent "<NEW_GUID>" {
//          }
//   4. Mod the player map menu prefab so its `MapConfiguration` field references
//      the GUID of THIS config instead of vanilla's.
//
// The overlay is gated client-side by SCR_EditorManagerEntity.IsOpened(), so
// installing this config globally has no effect for non-GM players.

// TODO: paste vanilla MapConfig block here, then add overlay entry below.
//
// MapConfig "<GUID>" {
//     ...
//     MapUIComponents {
//         ...vanilla components...
//         GME_GMFullscreenMapOverlayUIComponent "<NEW_GUID>" {}
//     }
// }
